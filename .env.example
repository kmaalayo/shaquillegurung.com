# Copy to .env for local development (never commit your real .env).

# Local dev port. Railway sets PORT automatically in production, so leave this
# out there. Locally we avoid 3000 (ReTeach) and 3100 (ClaimSaathi).
PORT=5050

# Contact form email delivery — the next increment (see README "Roadmap").
# Get a key at https://resend.com (you already use Resend for NoxNocTech).
# RESEND_API_KEY=
# CONTACT_TO=you@shaquillegurung.com
